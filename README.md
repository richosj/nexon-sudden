# nexon-sudden

서든어택 21주년 이벤트 페이지 퍼블리싱

## 로컬

```bash
npm install
npm run dev          # SCSS watch
npm run scss         # SCSS 1회 빌드
```

## 배포

GitHub Pages — `main` push 시 Actions에서 SCSS 빌드 후 루트(`index.html`) 배포

`dist/` 폴더는 사용하지 않습니다.
